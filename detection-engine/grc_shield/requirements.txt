# GRC-Shield Detection Engine — Dependencies
# Python 3.11+

# Core — no external dependencies for GS-01, GS-02, GS-03
# All three controls use only Python standard library:
# hashlib, sqlite3, json, dataclasses, datetime, pathlib, enum

# Optional — for the LangGraph demo (Layer 3)
# langgraph>=0.1.0
# langchain>=0.2.0
# langchain-anthropic>=0.1.0

# Optional — for FastAPI review dashboard
# fastapi>=0.111.0
# uvicorn>=0.29.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
